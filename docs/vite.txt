
### **Solução 2: Importando Imagens no JavaScript (Recomendado)**

Uma abordagem mais robusta é importar as imagens diretamente no JavaScript e injetá-las no HTML. Isso garante que o Vite lide corretamente com os caminhos durante o build.

#### **Passo 1: Importe as Imagens no JavaScript**
No arquivo `scripts.js` da sua página, importe as imagens:

```javascript
// Importando as imagens
import album1Cover from '../../img/Album 1/folder.jpg';
import album2Cover from '../../img/Album 2/folder.jpg';

// Injetando as imagens no HTML
document.querySelector('.album-cover img').src = album1Cover;
document.querySelector('.reverse .album-cover img').src = album2Cover;
```

#### **Passo 2: Atualize o HTML**
Remova os caminhos das imagens do HTML e adicione classes ou IDs para facilitar a seleção:

```html
<a href="/pags/Galeria/galeria.html" class="album-cover" tabindex="0">
    <img class="album1-cover" alt="Retrato..." loading="lazy">
</a>

<div class="album reverse">
    <div class="album-description">
        <h3 data-i18n="album2Title">Álbum 2: Casamentos</h3>
        <p data-i18n="album2Description">Momentos especiais de cerimônias e festas de casamento.</p>
    </div>
    <div class="album-cover" data-gallery="gallery2" tabindex="0">
        <img class="album2-cover" alt="Noivos se beijando em uma cerimônia de casamento" loading="lazy">
    </div>
</div>
```

#### **Passo 3: Atualize o JavaScript**
No arquivo `scripts.js`, selecione as imagens e atribua os caminhos:

```javascript
// Seleciona as imagens
const album1Img = document.querySelector('.album1-cover');
const album2Img = document.querySelector('.album2-cover');

// Atribui os caminhos das imagens
album1Img.src = album1Cover;
album2Img.src = album2Cover;
```

---

### **Solução 3: Usando Alias do Vite**

Se você configurou um alias para a pasta `img` no `vite.config.js`, pode usá-lo para referenciar as imagens.

#### **Passo 1: Configure o Alias no `vite.config.js`**
No arquivo `vite.config.js`, adicione um alias para a pasta `img`:

```javascript
import { defineConfig } from 'vite';
import path from 'path';

export default defineConfig({
    resolve: {
        alias: {
            '@img': path.resolve(__dirname, 'src/img'), // Alias para a pasta de imagens
        },
    },
});
```

#### **Passo 2: Use o Alias no JavaScript**
No arquivo `scripts.js`, importe as imagens usando o alias:

```javascript
import album1Cover from '@img/Album 1/folder.jpg';
import album2Cover from '@img/Album 2/folder.jpg';

// Injetando as imagens no HTML
document.querySelector('.album-cover img').src = album1Cover;
document.querySelector('.reverse .album-cover img').src = album2Cover;
```

---

### **Solução 4: Usando `public/` para Imagens Estáticas**

Se você não quer importar as imagens no JavaScript, pode mover as imagens para a pasta `public/`. Arquivos na pasta `public/` são servidos diretamente e não são processados pelo Vite.

#### **Passo 1: Mova as Imagens para `public/`**
Crie uma pasta `public/img/` e mova as imagens para lá:

```
public/
└── img/
    ├── Album 1/
    │   └── folder.jpg
    └── Album 2/
        └── folder.jpg
```

#### **Passo 2: Atualize os Caminhos no HTML**
No HTML, use caminhos absolutos a partir da raiz:

```html
<a href="/pags/Galeria/galeria.html" class="album-cover" tabindex="0">
    <img src="/img/Album 1/folder.jpg" alt="Retrato..." loading="lazy">
</a>

<div class="album reverse">
    <div class="album-description">
        <h3 data-i18n="album2Title">Álbum 2: Casamentos</h3>
        <p data-i18n="album2Description">Momentos especiais de cerimônias e festas de casamento.</p>
    </div>
    <div class="album-cover" data-gallery="gallery2" tabindex="0">
        <img src="/img/Album 2/folder.jpg" alt="Noivos se beijando em uma cerimônia de casamento" loading="lazy">
    </div>
</div>
```

---

### **Resumo das Soluções**
1. **Caminhos Relativos**: Use `../../img/...` para acessar a pasta `src/img/`.
2. **Importar no JavaScript**: Recomendado para projetos com Vite.
3. **Alias do Vite**: Use `@img` para referenciar a pasta de imagens.
4. **Pasta `public/`**: Para imagens estáticas que não precisam ser processadas.

Escolha a solução que melhor se adapta ao seu projeto. Se ainda houver problemas, compartilhe mais detalhes! 😊